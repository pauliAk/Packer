trigger:
- none

pool:
  vmImage: ubuntu-latest

steps:
- script: packer init .
#- task: packer build 'azureimage.pkr.hcl'
    
- task: PowerShell@2
  displayName: 'Packer build'
  inputs:
    targetType: 'inline'
    script: 'packer build $(build.artifactstagingdirectory)/azureimage.pkr.hcl'


 
    




